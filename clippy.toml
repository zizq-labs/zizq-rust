# Defence-in-depth against GHSA-jrvf-3jcg-q72w (time RFC 2822 parser
# stack-exhaustion advisory). This crate doesn't currently parse with
# `Rfc2822` anywhere, and the advisory was dismissed as not applicable;
# this guard ensures a future change can't silently introduce the
# vulnerable code path. Remove if time bumps past 0.3.46 in lockfile.
disallowed-types = [
    { path = "time::format_description::well_known::Rfc2822", reason = "Vulnerable parser (GHSA-jrvf-3jcg-q72w) — see clippy.toml header" },
]
